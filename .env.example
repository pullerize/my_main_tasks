# 🎯 8BIT Task Management System - Production Configuration Template
# Copy this file to .env and update with your actual values

# ==================== DATABASE CONFIGURATION ====================
# SQLite (for development/small deployments)
SQLITE_PATH=/path/to/your/shared_database.db
DATABASE_URL=sqlite:////path/to/your/shared_database.db

# PostgreSQL (recommended for production)
# Uncomment and configure these for production:
DB_ENGINE=postgresql
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=8bit_tasks
POSTGRES_USER=8bit_user
POSTGRES_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# ==================== TELEGRAM BOT ====================
# Get token from @BotFather
BOT_TOKEN=your-telegram-bot-token-here

# ==================== WEB APPLICATION ====================
# Generate secure key: openssl rand -hex 32
SECRET_KEY=CHANGE_THIS_TO_SECURE_RANDOM_STRING_32_CHARS

# ==================== DEPLOYMENT ====================
ENVIRONMENT=production
WEB_HOST=0.0.0.0
WEB_PORT=8000

# CORS (production domain: 8bit-task.site)
CORS_ORIGINS=https://8bit-task.site,https://www.8bit-task.site,http://8bit-task.site

# ==================== NOTES ====================
# 1. NEVER commit .env with real credentials to git
# 2. Use strong passwords for production
# 3. Update CORS_ORIGINS with your actual domain
# 4. For PostgreSQL, use db as host in docker-compose
